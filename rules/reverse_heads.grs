rule rev_head {
	pattern {e:H -[PUTFYLL]-> D; H[upos= ADP|CCONJ|PART|SCONJ]}
    without{D[upos= ADP|CCONJ|PART|SCONJ]}
    commands {
   %   e.rev=y;
      add_edge f:D -> H; f.label = e.label;
      del_edge e;
  %    shift_in H =[^IDIOM|TITLE|CONV]=> D;
  %    shift_out H =[1=case|mark]=> D;
  }
}