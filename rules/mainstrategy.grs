

import "relabel_NDT_to_UD_deprel.grs"
import "shift_root.grs"
import "reverse_heads.grs"


strat rules {
  Seq (

    %%% restructure graph
    Onf (reverse_heads),
    Onf (shift_root),
    Onf(shift_root.shift_copula_SPRED_head),  % <- already a sequence strategy

    %%% relabel edges
    Onf (relabel_NDT_to_UD_deprel),

 )
}

strat main { rules }

