% import rules from other grs files, which are treated as packages: 

import "testrules.grs"

import "shift_sentence_root.grs"
import "passive.grs"

strat test { 
  Seq (
    shift_sentence_root.shift_copula_SPRED_head,  % <- already a sequence strategy 
%    Onf (passive.passive_construction),
%    Onf (),
 )
}
  

strat main { Onf (test) }  
